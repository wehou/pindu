# PhonicsQuest 重构设计：以英式 48 音标为主干的「音 → 形」反向记忆体系

> 目标转向：不再"看字母猜读音"（拼写 → 音），而是"听到音、写出形"（音标 → 拼写），
> 用 20 元音 + 28 辅音做主干，把每一个音标变成"可检索、可练习、可验证"的单元。

---

## 一、现行架构审计结论

现行页面（3725 行单文件）骨架：

| 层 | 现状 |
|---|---|
| 数据层 | `GROUPS`(67 条音素/连缀) · `LETTER_SOUNDS`(26) · `PH_PAT`(20 条拼写模式) · `CVC_WORDS`(16) · `CCVC_WORDS`(19) · `MIN_PAIRS`(22) · `STRESS_WORDS`(35) · `SILENT_WORDS` · `SUFFIX_Q` · `TRICKY_WORDS`(78) |
| 组织层 | `STAGES`(10 阶段 / 295 知识点)，由 `LPT/PPT/TPT/BPT/CPT` 把上面数据装配成 point |
| 关卡层 | `stageLevels(st)` 按 `ptGrp` 分组 → `chunkRun(maxQ=14)` 切关 → 追加"混合复习"关 |
| 出题层 | `QGEN[kind](p, st, i)`，13 种题型，按知识点穷举生成 |
| 表现层 | `VIEWS.stage` 三 Tab（玩儿法 / 学一学 / 练一练）+ home/review/shadow |

**六个必须改的点：**

1. **主干错位**：阶段是按"拼写模式"推进（字母音 → CVC → 二合字母 → 连缀 → 魔法 e → 元音组 → r 控制 → 音节 → 词尾 → 静默字母），本质是"拼写 → 音"。音标只是音素卡里的一个字段，不是组织维度。
2. **音素库不纯**：67 条里 22 条不是音素——`bl/kl/fl/gl/pl/br/kr/fr/gr/pr/tr/sk/sm/sn/sp/st/sw/tw` 是**辅音连缀**，`ks/kw/ŋk` 是**二合一音组**。它们会污染"按音标检索"。真正缺失的音素只有 `/ts/ /dz/`（`/ɡ/` 已存在，只是用了 `ɡ` U+0261 字形）。
3. **没有反向主线**：音素数据其实已带 `combos[]` 和每词 `c`（该词用的是哪个拼写），"音标 → 拼写 → 例词"的数据基础是有的，但**呈现顺序仍是正向**（先给字母组/拼写模式，再举例词），没有"先给音，再给高频拼写"的反向视图，也没有正向/反向分栏切换。
4. **无 ⭐ 最高频标记**：`combos` 是平铺数组，没有频次信息。
5. **练习不是拼写产出**：现在是"按知识点生成选择题"，不是"听音 → 拆音素 → 选拼写 → 核对"。多音节重音只有 1 类题（35 词），没有逐音素拼写。
6. **音频与符号**：`speak()` 的 `pickVoice()` 只偏好 `en-GB`，无 UK/US 显式切换；`ː ˈ ˌ` 只是普通字符串，没有专门渲染与数据校验。

---

## 二、新架构总览（五层）

```
┌─ 配置层 CONFIG（纯数据，可独立替换/扩展）───────────────┐
│  PHONEMES   48 音标（20 元音 + 28 辅音）                │
│  SPELLINGS  拼写 → 音 的正向规则（一拼多音在此收敛）    │
│  WORDS      练习词库（含音素切分 / 音节 / 重音 / 词义） │
│  LEVELS     5 组难度递进关卡路径                        │
│  EXCEPTIONS ough / 不发音字母 / 同音异形                │
└──────────────────────────────────────────────────────┘
                        ↓ 构建索引
┌─ 索引层 INDEX（启动时 O(n) 预建，运行时 O(1) 查询）────┐
│  BY_IPA  /iː/ → phoneme                                │
│  BY_SPELL  ee → [phoneme...]（一个拼写可能对应多音）    │
│  BY_DIFF   L1..L5 → [phoneme...]                       │
│  BY_GROUP  mono/diph/plosive/... → [phoneme...]        │
└──────────────────────────────────────────────────────┘
                        ↓
┌─ 学习层 LEARN ─┐   ┌─ 练习层 DRILL ───────────────────┐
│ 音标详情       │   │ 关卡列表（5 组）                 │
│  ├ 反向（默认）│   │ 单题 4 步：                       │
│  │  音→⭐拼写 │   │  ① 听音  ② 拆音素                │
│  │     → 例词 │   │  ③ 选拼写 ④ 核对修正             │
│  └ 正向（切换）│   │ 单音节 /kæt/→cat                │
│     拼写→音   │   │ 多音节 /ˌɪntəˈnæʃənəl/ 带重音    │
│     +条件/例外│   │ 结果回写掌握度 + 错题本           │
└────────────────┘   └──────────────────────────────────┘
                        ↓
┌─ 表现层 UI ──────────────────────────────────────────┐
│  ipaHtml()  把 /ˌɪntəˈnæʃənəl/ 渲染为带 ˈ ˌ ː 的符号 │
│  speak(text,{accent:'UK'|'US'})  en-GB / en-US 切换  │
│  shadow()   跟读录音对比                              │
└──────────────────────────────────────────────────────┘
```

---

## 三、数据模型

### 3.1 PHONEMES（48 音标）—— 体系主干

```js
{
  ipa: '/iː/', key: 'i:',                 // 展示用 / 索引键
  cls: 'vowel' | 'consonant',
  vtype: 'mono' | 'diph' | null,          // 单元音 / 双元音（仅元音）
  len:  'long' | 'short' | null,          // 长音(ː) / 短音（仅单元音）
  voiced: true|false|null, pair: '/ɪ/',   // 清浊成对（仅辅音；pair 互指）
  name: '长元音 /iː/',

  // ⭐ 反向主线的核心：拼写按频次排序，star=1 标记最高频写法
  spellings: [
    { s:'ee', star:1, note:'最常见，多在词中/词尾' },
    { s:'ea', note:'tea/sea/meat，遇 ea 先想 /iː/' },
    { s:'e',  note:'开音节 he/she/we/be' },
    { s:'ie', note:'field/believe（例外：friend 读 /e/）' },
    { s:'ei', note:'receive/ceiling' }
  ],

  // 例词：反向路径的终点（音 → 拼写 → 词 → 义）
  words: [
    { w:'see',  ipa:'/siː/',  sp:'ee', zh:'看见', syl:1 },
    { w:'tea',  ipa:'/tiː/',  sp:'ea', zh:'茶',   syl:1 },
    { w:'these',ipa:'/ðiːz/', sp:'e',  zh:'这些', syl:1 }
  ],

  // 正向规则（字母 → 音）：给"看词读音"时用
  say: '嘴角向两侧展开，舌位最高，持续长音',
  vs:  '⚠️ 与 /ɪ/ 只差长短与舌位：sheep /ʃiːp/ vs ship /ʃɪp/',

  // 位置规则（第 4 组关卡用）
  posRule: null | { title:'ai / ay', body:'ai 在词中（rain），ay 在词尾（day）' },

  // 例外（就近挂在音标上，详情页底部展示）
  exc: ['friend 中 ie 读 /e/，不是 /iː/']
}
```

**48 音标清单**

- **12 单元音**：/iː/ /ɪ/ /e/ /æ/ /ɑː/ /ɒ/ /ɔː/ /ʊ/ /uː/ /ʌ/ /ɜː/ /ə/
- **8 双元音**：/eɪ/ /aɪ/ /ɔɪ/ /əʊ/ /aʊ/ /ɪə/ /eə/ /ʊə/
- **28 辅音**：/p/ /b/ /t/ /d/ /k/ /g/ /f/ /v/ /s/ /z/ /θ/ /ð/ /ʃ/ /ʒ/ /tʃ/ /dʒ/ /tr/ /dr/ /ts/ /dz/ /m/ /n/ /ŋ/ /h/ /l/ /r/ /w/ /j/

> 说明：采用国内教材的 48 音标口径，`/tr/ /dr/ /ts/ /dz/` 独立计为音素；
> 现行数据里的 `bl/kl/br/sk/...` 等**连缀**不进音素表，改挂到 `CLUSTERS`（音素组合层），
> `ks/kw/ŋk` 同理——它们由 2 个音素拼成，练习时按"拆音素"处理，而不是当成一个音去背拼写。

### 3.2 SPELLINGS（正向规则，由 PHONEMES 反自动生成 + 少量手工补）

```
ee  → /iː/（主）; 见 PHONEMES 反向表
ea  → /iː/（主）/ /e/（head）/ /eɪ/（break）   ← 一拼多音在此收敛
ough→ /ɔː/(bought) /aʊ/(plough) /ʌf/(rough) /əʊ/(though) /uː/(through)
```

### 3.3 WORDS（练习词库）

```js
{
  w:'international', ipa:'/ˌɪntəˈnæʃənəl/', zh:'国际的',
  syl:['in','ter','na','tion','al'], stress:2,      // 主重音在第 3 音节(0基)
  soft:0,                                            // 次重音位置
  ph:[ {ipa:'/ɪ/',sp:'i'}, {ipa:'/n/',sp:'n'}, ... ], // 逐音素切分（供第②步拆音素）
  diff: 5, lv:'L5'
}
```
单音节同样结构（`syl:1`，如 `cat /kæt/ → ph:[/k/→c, /æ/→a, /t/→t]`）。

### 3.4 LEVELS（关卡路径，5 组难度递进）

| 组 | 主题 | 选入音标 | 认知依据 |
|---|---|---|---|
| **G1** | 唯一拼写音 | /θ/ /ð/ /tr/ /dr/ /ts/ /dz/ /ŋ/ /h/ /w/ /j/ | 一音基本只有一种写法，先建立"音↔形一对一"的信心 |
| **G2** | 清浊成对辅音 | /p/-/b/ /t/-/d/ /k/-/g/ /f/-/v/ /s/-/z/ /ʃ/-/ʒ/ /tʃ/-/dʒ/ | 用最小对立对练听辨，成对记 |
| **G3** | 长短元音 + 双元音 | /iː/-/ɪ/ /ɑː/-/ʌ/ /ɔː/-/ɒ/ /uː/-/ʊ/ /ɜː/-/ə/ + 8 双元音 | 元音是拼写歧义最大处，放在有辅音基础之后 |
| **G4** | 位置规则 | ai/ay（词中/词尾）、c（a o u 前 /k/ vs e i y 前 /s/）、g（软/硬） | 同一音的写法由"位置"决定 |
| **G5** | 开闭音节 | a_e/ee/... 开音节长音 vs 闭音节短音 | 综合规则，最后收网 |

### 3.5 EXCEPTIONS

```js
{ ough:[{w:'bought',ipa:'/bɔːt/',zh:'买(过去)'}, ...],
  silent:[{w:'knife',dead:'k',zh:'刀'}, ...],
  homophone:[{a:'sea',b:'see',ipa:'/siː/',zhA:'大海',zhB:'看见'}, ...] }
```

---

## 四、学习主线：反向为主，正向可切

音标详情页 = **一屏两视图**，Tab 切换，默认落在**反向**：

```
┌─────────────────────────────────────────────┐
│  /iː/   长元音 · 长音        [UK] [US] 🔊   │
├─────────────────────────────────────────────┤
│ [反向 音→形] [正向 形→音]   ← 默认=反向    │
├─────────────────────────────────────────────┤
│ 反向：                                       │
│  ⭐ ee  ← 最高频写法                         │
│     ea · e · ie · ei                        │
│  例词：see /siː/ 看见 · tea /tiː/ 茶 ...     │
├─────────────────────────────────────────────┤
│ 正向（切到这栏）：                           │
│  ee → /iː/  see/seed/feet                   │
│  ea → /iː/  tea/sea   ⚠️ 例外 head 读 /e/   │
├─────────────────────────────────────────────┤
│ ⚠️ 例外：friend 里 ie 读 /e/                 │
└─────────────────────────────────────────────┘
```

---

## 五、核心玩法：听音 → 拆音素 → 选拼写 → 核对修正

以 `/kæt/` → `cat` 为例，一题 4 步：

1. **听音**：播放单词（UK/US 可切），只给音 **不给形**。
2. **拆音素**：屏幕上出现音标 `/kæt/`，逐音位点选/填入切分 → `k | æ | t`。
3. **选拼写**：每个音素选对应拼写（`/k/` 选 `c` 而非 `k`/`ck`；`/æ/` 选 `a`；`/t/` 选 `t`）——⭐ 写法在选项里做弱提示（不直接给答案）。
4. **核对修正**：拼出 `cat` → 展示 **完整音标 + 拼写拆解 + 词义**；错了逐音素标红并给"为什么"（如 /k/ 在此位置 99% 写 c，只有词尾才写 ck）。

多音节 `/ˌɪntəˈnæʃənəl/` → `international` 同样 4 步，额外要求标**主重音**（第 3 音节）与**次重音**，拆音节 `in-ter-na-tion-al`。

每题结果回写 `S.prog[pid]`（掌握度），错题进错题本并进间隔复习队列。

---

## 六、音频与符号渲染

- **UK/US**：`speak(text,{accent})` → `pickVoice()` 按 `en-GB` / `en-US` 选声；顶部全局切换，音标卡与练习共用。
- **符号渲染**：`ipaHtml(ipa)` 把 `/ˌɪntəˈnæʃənəl/` 解析后逐字符渲染——`ˈ` 主重音（粗+上标色）、`ˌ` 次重音（浅一档）、`ː` 长音（加宽并高亮），保证在移动端不串行。
- **数据校验**：启动时跑 `validateData()`——长元音必须含 `ː`；`stress` 索引必须落在 `syl` 范围内；`ph[]` 的音素拼接应能覆盖 IPA 主体。控制台报错，开发期即可发现。

---

## 七、检索与扩展

配置层一旦独立，即可按任意维度取数（无需改逻辑）：

```js
byIpa('/iː/')        // 单个音标
bySpelling('ough')   // 一拼多音 → 返回所有可能音
byDiff(3)            // 某难度全部音标
byGroup('diph')      // 全部双元音
query({cls:'vowel', len:'long'})   // 组合查询
```
新增一个音标 = 往 `PHONEMES` 加一条；新增关卡 = 往 `LEVELS` 加一组；新增题型 = 加一个 `QGEN`。互不侵入。
